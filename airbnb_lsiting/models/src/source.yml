version: 2

sources:
  - name: src_airbnb
    database: 
    schema: RAW
    config:
      enabled: true
    tables:
      - name: RAW_HOSTS
      - name: RAW_LISTINGS
      - name: RAW_REVIEWS

